services:
  redis:
    image: redis:7-alpine
    container_name: rede_pecas_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1

volumes:
  redis_data:
