// Domain-specific Joi custom validators go here as new schemas need them
// (e.g. VIN format, phone number format). None are required yet — Joi's
// built-in string/number rules cover the schemas defined so far.
export {};
